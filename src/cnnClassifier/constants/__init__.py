#this constant folder is for the constant files that won't be changing and we will pass the path of it

from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")
